# @cloudproof/plugin-github-actions

Detector de GitHub Actions (`.github/workflows/`) y mapeo de un `CloudProofBundle` a un resumen de Check Run. La publicación real (llamada a la GitHub Checks API) requiere la GitHub App del repositorio privado `cloudproof-cloud` y es Fase 2.
