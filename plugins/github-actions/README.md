# @proof/plugin-github-actions

Detector de GitHub Actions (`.github/workflows/`) y mapeo de un `ProofBundle` a un resumen de Check Run. La publicación real (llamada a la GitHub Checks API) requiere la GitHub App del repositorio privado `proof-cloud` y es Fase 2.
